# AGENTS.md

## Workspace state

Fresh workspace — no code or tooling configured yet. First project creation determines all conventions.

## Local tooling

Ollama runs locally at `http://localhost:11434/v1`. Model config in `~/.config/opencode/opencode.json`.
